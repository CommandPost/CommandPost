#include "json2.js";if(app.preferences.getPrefAsLong('Main Pref Section v2', 'Pref_SCRIPTING_FILE_NETWORK_SECURITY', PREFType.PREF_Type_MACHINE_SPECIFIC) != 1){	alert('Allow Scripts to Write Files and Access Network is turned off.\nCommandPost requires permission to write files, so that it can save a list of all your installed plugins. Please turn on this preference in the Scripting & Expressions tab of Preferences and try again.');} else {	var effects = new Array();	var curEffect;	var allEffects = app.effects;	var allEffectsLen = allEffects.length;	for(var e=0; e<allEffectsLen; e++){		curEffect = allEffects[e];		effects.push({ displayName: curEffect.displayName, matchName: curEffect.matchName, category: curEffect.category });	}	var data = JSON.stringify(effects);	var doc = new File('~/Library/Caches/org.latenitefilms.CommandPost/After Effects/Effects.cpCache');	doc.open('w');	doc.write(data);	doc.close();}